require('globals')      -- global variables
require('options')      -- nvim basic settings

require('manager')      -- plugin manager

require('keys/main')    -- keybindings for nvim
require('keys/plugins') -- keybindings for plugins

require('color')        -- color scheme
